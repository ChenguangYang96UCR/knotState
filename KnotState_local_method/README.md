# Local KnotState

See [the implementation guide](knot_local_method/README.md) for the method, step-by-step commands, input formats, evaluation protocol, and tests.

Run all commands from this directory. The importable Python package is `knot_local_method`.
